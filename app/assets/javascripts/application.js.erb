//= require jquery
//= require main.js
//= require_self

var csrf = '<%= "#{ENV['VGS_CSRF']}" %>'

// configure everything when jquery is ready
$(function () {
    BikeRental.init({
        csrfToken: csrf
    });
});

